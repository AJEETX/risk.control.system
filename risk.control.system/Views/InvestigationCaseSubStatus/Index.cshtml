@model IEnumerable<risk.control.system.Models.InvestigationCaseSubStatus>

@{
    ViewData["Title"] = @Applicationsettings.SERVICE;
}
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-success">
                    <form asp-action="index" asp-antiforgery="true">
                        <div class="card-header">
                            <h3 class="card-title"><i class="fas fa-folder-open"></i> @Applicationsettings.CASE_SUBSTATUS</h3>
                            <div class="card-tools">
                                <div class="input-group input-group-sm">
                                    <input autocomplete="off" name="SearchString" type="text" class="form-control" placeholder="search">
                                    <div class="input-group-append">
                                        <div onclick="this.parentNode.submit()" class="btn btn-success">
                                            <i class="fas fa-search"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="card-body">
                           
                            <div class="row">
                                <div class="col-md-12 text-right">
                                    <a class="btn btn-success" asp-action="Create"><i class="fas fa-plus"></i>  @Applicationsettings.CREATE</a>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="card card-success">
                    <div class="card-body p-0 contained-width">
                        @if (Model.Count() == 0)
                        {
                            <hr />
                            <div class="col-md-12 text-center">
                                <caption>@Applicationsettings.NO_RECORDS_FOUND</caption>
                            </div>
                            <hr />
                        }
                        else
                        {
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>
                                            @Html.DisplayNameFor(model => model.Name)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.Code)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.InvestigationCaseStatus)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.Created)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.Updated)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.UpdatedBy)
                                        </th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        var masterData = item.MasterData ? "none" : "";
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Name)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Code)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.InvestigationCaseStatus.Name)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Created)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Updated)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.UpdatedBy)
                                            </td>
                                            <td>
                                                <a class="btn btn-xs btn-info" asp-action="Details" asp-route-id="@item.InvestigationCaseSubStatusId">
                                                    <i class="fa fa-search"></i>  @Applicationsettings.VIEW
                                                </a> &nbsp;
                                                <a class="btn btn-xs btn-warning" style="pointer-events:@masterData" asp-action="Edit" asp-route-id="@item.InvestigationCaseSubStatusId">
                                                    <i class="fas fa-pen"></i>  @Applicationsettings.EDIT
                                                </a> &nbsp;
                                                <a class="btn btn-xs btn-danger" style="pointer-events:@masterData" asp-action="Delete" asp-route-id="@item.InvestigationCaseSubStatusId">
                                                    <i class="fas fa-trash"></i>  @Applicationsettings.DELETE
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>