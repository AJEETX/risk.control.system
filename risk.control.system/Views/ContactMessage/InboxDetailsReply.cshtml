@model risk.control.system.Models.OutboxMessage

@{
    ViewData["Title"] = "Reply";
    
    <link href="~/plugins/summernote/summernote.css" rel="stylesheet" />

}
<section class="content">
    <form asp-controller="ContactMessage" id="email-form" method="post" enctype="multipart/form-data">
        <div class="card card-secondary">
            <input type="hidden" asp-for="OutboxMessageId" />
            <div class="card-header">
                <h3 class="card-title">@ViewBag.ActionType Mail</h3>
            </div>
            <div class="card-body">
                <div class="submit-progress hidden">
                    <i class="fas fa-sync fa-spin fa-4x fa-fw"></i>
                </div>
                <div class="form-group">
                    <input id="receipient-email" class="form-control remarks" asp-for="ReceipientEmail" value="@Model.ReceipientEmail">
                </div>
                <div class="form-group">
                    <input class="form-control remarks" asp-for="Subject" value="@Model.Subject">
                </div>
                <div style="display:none" class="form-group">
                    <select asp-for="Priority" asp-items="Html.GetEnumSelectList<ContactMessagePriority>()">
                    </select>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.RawMessage, new { @class = "col-md-12 control-label" })
                    <div class="col-md-12">
                        @Html.TextAreaFor(model => model.RawMessage, new { @class = "form-control remarks", @row = 5 })
                    </div>
                </div>
                <div class="form-group">
                    

                </div>
            </div>

            <div class="card-footer">
                <div class="btn btn-default btn-file">
                    <i class="fas fa-paperclip"></i> Attachment
                    <img class="file-image doc-profile-image" id="documentImage0" src=@Applicationsettings.NO_IMAGE>
                    <input id="document" name="document" asp-for="Attachment" type="file" accept="*.png, *.jpeg" class="form-control remarks" />
                </div>
                <span class="help-block">Max. 2MB</span>
                <div class="float-right">
                    <a asp-controller="ContactMessage" asp-action="InboxDetails" asp-route-id="@ViewBag.MessageId" type="reset" class="btn btn-secondary"><i class="fas fa-times"></i> @Applicationsettings.CANCEL</a>
                    <button id="send-email" type="submit" asp-action="InboxDetailsReply" class="btn btn-success"><i class="far fa-envelope"></i> Reply</button>
                </div>
            </div>

        </div>
    </form>

</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/plugins/summernote/summernote.js"></script>
    <script src="~/js/compose-mail.js">
    </script>
}
