@using risk.control.system.Helpers;
@model CaseInvestigationVendorsModel
<div class="card shadow-lg border-1 rounded-lg">
    <div class="card-header bg-light text-white">
        <h3 class="card-title">
            <i class="fas fa-user-shield"></i> Report (@Model.ClaimsInvestigation?.PolicyDetail.InsuranceType.GetEnumDisplayName()): <span class="badge badge-light"> @Model.InvestigationReport.AgentEmail </span>
            <span class="hover-text">
                <i class="fas fa-info-circle"></i>
                <span class="tooltip-text" id="information">
                    <span>

                        <i class="fas fa-user-shield"></i><b> @Model.InvestigationReport.AgentEmail </b> report
                    </span>
                </span>
            </span>
        </h3>
        <div class="card-tools">
            <span data-toggle="tooltip" title="Agent (@Model.ClaimsInvestigation?.PolicyDetail.InsuranceType.GetEnumDisplayName() ) report: @Model.InvestigationReport.AgentEmail" class="badge badge-light"><i class="fas fa-user-shield"></i></span>
        </div>
    </div>


    <div class="card-body pb-0">
        <div class="submit-progress hidden">
            <i class="fas fa-sync fa-spin fa-4x fa-fw"></i>
        </div>
        <div class="row">

            <partial name="_AgentIdReport" model="@Model" />
            <partial name="_DigitalIdReport" model="@Model" />
            <partial name="_DocumentIdReport" model="@Model" />
            
        </div>
        <div class="row">
            <partial name="_ClaimQuestionaire" model="@Model" />
        </div>
    </div>
</div>
