@model IEnumerable<risk.control.system.Models.Vendor>

@{
    ViewData["Title"] = @Applicationsettings.SERVICE;
}

<section class="content pt-2">
    <div class="container-fluid">
        
        <div class="row">
            <div class="col-md-12">
                <div class="card card-secondary">
                    <div class="card-header">
                        <h3 class="card-title"><i class="fas fa-handshake"></i> Empanelled Agencies</h3>
                    </div>
                    <div class="card-body">
                        <form id="checkboxes" method="post" asp-action="EmpanelledVendors">
                                <table id="datatable" class="table table-bordered table-hover dt-responsive nowrap w-100">
                                    <thead>
                                        <tr>
                                            <th><input type="checkbox" id="checkall" /></th>
                                        
                                            <th>
                                                @Html.DisplayNameFor(model => model.Document)
                                            </th>
                                        <th>
                                            Domain
                                        </th>
                                            <th>
                                Name
                            </th>
                            <th>
                                Code
                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.PhoneNumber)
                                            </th>
                                            
                                            <th>
                                                @Html.DisplayNameFor(model => model.Addressline)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.State)
                                            </th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>
                                                    <input id="@item.VendorId" class="vendors" name="vendors" type="checkbox" value="@item.VendorId" checked="@item.SelectedByCompany" />
                                                </td>
                                                <td>
                                                    @if (item?.DocumentUrl is not null)
                                                    {
                                                        <img id="documentImage" class="table-row-image" src="@item?.DocumentUrl">
                                                    }
                                                    else
                                                    {
                                                        <img id="documentImage" class="table-row-image" src="@Applicationsettings.NO_IMAGE">
                                                    }
                                                </td>
                                            <td>
                                                <a href=""> @Html.DisplayFor(modelItem => item.Email)</a>
                                            </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Name)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Code)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.PhoneNumber)
                                                </td>
                                                
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Addressline)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.State.Name)
                                                </td>
                                                <td>
                                                    <a class="btn btn-xs btn-info" asp-action="VendorDetails" asp-route-backurl="EmpanelledVendors" asp-route-id="@item.VendorId">
                                                        <i class="fa fa-search"></i> @Applicationsettings.DETAILS
                                                    </a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                                <div class="row">
                                    <div class="col-md-12">

                                        <button disabled="disabled" id="manage-vendors" type="submit" class="btn btn-danger">
                                        <i class="fas fa-hand-pointer" aria-hidden="true"></i> Depanel Agency(s)
                                        </button>
                                    </div>
                                </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>